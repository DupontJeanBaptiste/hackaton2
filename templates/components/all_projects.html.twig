{% for project in this.allProjects %}

	{{component('project', {
    id: project.id,
    name: project.name,
    description: project.description,
    client: project.client,
    users: project.users
    })}}

{% endfor %}
